{
  "devDependencies": {
    "husky": "^8.0.0",
    "prettier": "^3.5.1"
  },
  "scripts": {
    "format": "prettier --write '**/*.{js,css,html,md}'",
    "lint:html": "htmlhint '**/*.html'",
    "prepare": "husky install"
  }
}
